# Followup_plus
A health tracking app
## API Key Setup (For Demo)

This project uses Google Gemini API.
For security reasons, the API key is not included.

To run locally:
1. Get a Gemini API key from Google AI Studio
2. Replace GEMINI_KEY in the script section

⚠️ In production, this should be handled via a backend proxy.
